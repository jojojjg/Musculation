const STORAGE_KEY='workout-tracker-static-v1';
const defaultPlans={training1:{name:'Entraînement 1',source:'Lundi',exercises:[{id:'t1e1',name:'Chest Press',muscle:'Pectoraux / Triceps',targetReps:10,plannedWeight:27.5,substitution:'Pompes au sol'},{id:'t1e2',name:'Tirage vertical',muscle:'Dos / Biceps',targetReps:10,plannedWeight:32.5,substitution:'Tractions assistées / élastique'},{id:'t1e3',name:'Presse à cuisses',muscle:'Quadriceps / Fessiers',targetReps:10,plannedWeight:75,substitution:'Goblet squat'},{id:'t1e4',name:'Rowing poulie basse',muscle:'Dos milieu / Posture',targetReps:10,plannedWeight:27.5,substitution:'Rowing haltères / élastique'},{id:'t1e5',name:'Oiseau',muscle:'Arrière épaules',targetReps:15,plannedWeight:6,substitution:'Élévations arrière haltères'},{id:'t1e6',name:'Curl biceps poulie basse',muscle:'Biceps / Avant-bras',targetReps:12,plannedWeight:11,substitution:'Curl haltères'},{id:'t1e7',name:'Gainage',muscle:'Core',targetReps:30,plannedWeight:0,unit:'sec',substitution:'Planche classique'}]},training2:{name:'Entraînement 2',source:'Mercredi',exercises:[{id:'t2e1',name:'Leg Curl',muscle:'Ischios',targetReps:12,plannedWeight:22.5,substitution:'Pont fessier / Swiss ball leg curl'},{id:'t2e2',name:'Chest Press',muscle:'Pectoraux / Triceps',targetReps:10,plannedWeight:27.5,substitution:'Pompes au sol'},{id:'t2e3',name:'Tirage vertical',muscle:'Dos / Biceps',targetReps:10,plannedWeight:32.5,substitution:'Tractions assistées / élastique'},{id:'t2e4',name:'Triceps poulie',muscle:'Triceps',targetReps:12,plannedWeight:13.5,substitution:'Dips banc / extension haltère'},{id:'t2e5',name:'Curl biceps poulie basse',muscle:'Biceps / Avant-bras',targetReps:12,plannedWeight:12.5,substitution:'Curl haltères'},{id:'t2e6',name:'Gainage',muscle:'Core',targetReps:30,plannedWeight:0,unit:'sec',substitution:'Planche classique'}]},training3:{name:'Entraînement 3',source:'Vendredi',exercises:[{id:'t3e1',name:'Presse à cuisses',muscle:'Quadriceps / Fessiers',targetReps:10,plannedWeight:75,substitution:'Goblet squat'},{id:'t3e2',name:'Rowing poulie basse',muscle:'Dos / Posture',targetReps:10,plannedWeight:27.5,substitution:'Rowing haltères / élastique'},{id:'t3e3',name:'Pec Fly',muscle:'Pectoraux',targetReps:12,plannedWeight:22.5,substitution:'Écarté haltères'},{id:'t3e4',name:'Tirage vertical',muscle:'Dos / Biceps',targetReps:10,plannedWeight:32.5,substitution:'Tractions assistées / élastique'},{id:'t3e5',name:'Oiseau',muscle:'Arrière épaules',targetReps:15,plannedWeight:6,substitution:'Élévations arrière haltères'},{id:'t3e6',name:'Curl marteau poulie',muscle:'Biceps / Avant-bras',targetReps:12,plannedWeight:11,substitution:'Curl marteau haltères'},{id:'t3e7',name:'Mollets',muscle:'Mollets',targetReps:15,plannedWeight:35,substitution:'Mollets debout poids du corps'}]}};
const initialState={selectedTraining:'training1',plans:defaultPlans,currentEntries:{},history:[]};
function todayKey(date=new Date()){const local=new Date(date);local.setMinutes(local.getMinutes()-local.getTimezoneOffset());return local.toISOString().slice(0,10)}
function formatDateFr(dateStr){return new Date(dateStr).toLocaleDateString('fr-FR',{day:'2-digit',month:'2-digit',year:'numeric'})}
function loadState(){try{const raw=localStorage.getItem(STORAGE_KEY);return raw?{...initialState,...JSON.parse(raw)}:initialState}catch{return initialState}}
let state=loadState();
function saveState(){localStorage.setItem(STORAGE_KEY,JSON.stringify(state))}
function setState(updater){state=typeof updater==='function'?updater(state):updater;saveState();render()}
function bestLoggedExercise(){const counts={};state.history.forEach(s=>s.exercises.forEach(ex=>counts[ex.name]=(counts[ex.name]||0)+1));const best=Object.entries(counts).sort((a,b)=>b[1]-a[1])[0];return best?`${best[0]} (${best[1]})`:'Aucune donnée'}
function completionMap(){const map={};state.history.forEach(item=>{map[item.date]=(map[item.date]||0)+1});return map}
function renderDashboard(){const map=completionMap();const now=new Date();document.getElementById('statTotalSessions').textContent=state.history.length;document.getElementById('statMonthSessions').textContent=state.history.filter(h=>{const d=new Date(h.date);return d.getMonth()===now.getMonth()&&d.getFullYear()===now.getFullYear()}).length;document.getElementById('statLastSession').textContent=state.history[0]?.date?formatDateFr(state.history[0].date):'Aucune';document.getElementById('statBestExercise').textContent=bestLoggedExercise();document.getElementById('summaryTraining').textContent=state.plans[state.selectedTraining].name;document.getElementById('summaryExerciseCount').textContent=state.plans[state.selectedTraining].exercises.length;document.getElementById('summaryDays').textContent=Object.keys(map).length;document.getElementById('summaryToday').textContent=todayKey()}
function renderTrainingSelectors(){['training1','training2','training3'].forEach(key=>{document.getElementById(`${key}Name`).value=state.plans[key].name});document.getElementById('trainingSelect').value=state.selectedTraining;document.getElementById('selectedTrainingTitle').textContent=state.plans[state.selectedTraining].name;document.querySelectorAll('.training-select').forEach(btn=>btn.classList.toggle('btn-secondary',btn.dataset.training!==state.selectedTraining))}
function renderExercises(){const plan=state.plans[state.selectedTraining];const container=document.getElementById('exerciseList');container.innerHTML='';plan.exercises.forEach(ex=>{const current=state.currentEntries[ex.id]||{};const card=document.createElement('div');card.className='exercise-card';card.innerHTML=`<div class="exercise-main"><div class="exercise-top"><h3>${ex.name}</h3><span class="badge">${ex.muscle}</span></div><div class="exercise-meta">Objectif : ${ex.targetReps} ${ex.unit||'reps'} • Poids prévu : ${ex.plannedWeight===0?'PDC':ex.plannedWeight+' kg'}</div><div class="exercise-substitution">Substitution : ${ex.substitution}</div></div><label class="checkbox-wrap"><input type="checkbox" ${current.done?'checked':''}><span>Exercice effectué</span></label><div class="field-group"><label class="field-label">Nouveau poids utilisé</label><input class="input" type="number" step="0.5" placeholder="kg" value="${current.usedWeight??''}"></div><div class="field-group"><label class="field-label">Nombre de répétitions</label><input class="input" type="number" placeholder="${ex.unit||'reps'}" value="${current.reps??''}"></div>`;const checkbox=card.querySelector('input[type="checkbox"]');const inputs=card.querySelectorAll('.input');const weightInput=inputs[0];const repsInput=inputs[1];checkbox.addEventListener('change',e=>setState(prev=>({...prev,currentEntries:{...prev.currentEntries,[ex.id]:{...(prev.currentEntries[ex.id]||{}),done:e.target.checked}}})));weightInput.addEventListener('input',e=>setState(prev=>({...prev,currentEntries:{...prev.currentEntries,[ex.id]:{...(prev.currentEntries[ex.id]||{}),usedWeight:e.target.value}}})));repsInput.addEventListener('input',e=>setState(prev=>({...prev,currentEntries:{...prev.currentEntries,[ex.id]:{...(prev.currentEntries[ex.id]||{}),reps:e.target.value}}})));container.appendChild(card)})}
function renderCalendar(){const grid=document.getElementById('calendarGrid');grid.innerHTML='';const map=completionMap();const today=new Date();for(let i=83;i>=0;i--){const d=new Date(today);d.setDate(today.getDate()-i);const key=todayKey(d);const count=map[key]||0;const level=count>=3?'level-3':count===2?'level-2':count===1?'level-1':'level-0';const day=document.createElement('div');day.className='calendar-day';day.innerHTML=`<div class="calendar-cell ${level}" title="${formatDateFr(key)} • ${count} séance(s)"></div><div class="calendar-label">${d.getDate()}</div>`;grid.appendChild(day)}}
function renderHistory(){const list=document.getElementById('historyList');list.innerHTML='';if(!state.history.length){list.innerHTML='<div class="empty-state">Aucune séance enregistrée.</div>';return}state.history.forEach(session=>{const card=document.createElement('div');card.className='history-card';const exercises=session.exercises.map(ex=>`<div class="history-exercise"><span>${ex.name}</span><span>${ex.usedWeight||0} kg • ${ex.reps} reps</span></div>`).join('');card.innerHTML=`<div class="history-top"><div><div class="history-title">${session.trainingName}</div><div class="small-text">${formatDateFr(session.date)}</div></div><span class="badge">${session.exercises.length} exos</span></div><div class="history-exercises">${exercises}</div>`;list.appendChild(card)})}
function render(){renderTrainingSelectors();renderExercises();renderCalendar();renderHistory();renderDashboard()}
function initEvents(){document.getElementById('resetBtn').addEventListener('click',()=>{if(!confirm('Supprimer toutes les données locales ?'))return;localStorage.removeItem(STORAGE_KEY);state=JSON.parse(JSON.stringify(initialState));render()});['training1','training2','training3'].forEach(key=>document.getElementById(`${key}Name`).addEventListener('input',e=>setState(prev=>({...prev,plans:{...prev.plans,[key]:{...prev.plans[key],name:e.target.value}}}))));document.querySelectorAll('.training-select').forEach(btn=>btn.addEventListener('click',()=>setState(prev=>({...prev,selectedTraining:btn.dataset.training}))));document.getElementById('trainingSelect').addEventListener('change',e=>setState(prev=>({...prev,selectedTraining:e.target.value})));document.getElementById('saveSessionBtn').addEventListener('click',()=>{const plan=state.plans[state.selectedTraining];const sessionExercises=plan.exercises.filter(ex=>state.currentEntries[ex.id]?.done).map(ex=>({id:ex.id,name:ex.name,muscle:ex.muscle,plannedWeight:ex.plannedWeight,targetReps:ex.targetReps,usedWeight:Number(state.currentEntries[ex.id]?.usedWeight||0),reps:Number(state.currentEntries[ex.id]?.reps||0)}));if(!sessionExercises.length){alert('Aucun exercice coché pour cette séance.');return}const newSession={id:`${Date.now()}-${Math.random().toString(36).slice(2,8)}`,trainingId:state.selectedTraining,trainingName:plan.name,date:todayKey(),exercises:sessionExercises};setState(prev=>({...prev,history:[newSession,...prev.history],currentEntries:{}}));alert('Séance enregistrée.')});document.querySelectorAll('.tab').forEach(tab=>tab.addEventListener('click',()=>{document.querySelectorAll('.tab').forEach(t=>t.classList.remove('active'));tab.classList.add('active');const target=tab.dataset.tab;document.querySelectorAll('.tab-panel').forEach(p=>p.classList.add('hidden'));document.getElementById(`${target}Tab`).classList.remove('hidden')}))}
initEvents();render();
